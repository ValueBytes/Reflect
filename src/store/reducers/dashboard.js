const initState = {
  boards: [
    {
      title: 'waywo',
      columns: []
    }
  ]
};

const dashboardReducer = (state = initState, action) => {
  return state;
}

export default dashboardReducer;